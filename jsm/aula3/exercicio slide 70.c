#include "rims.h"

/* exercicio slide 70
   autor:Jonathan Soares
Salve, compile e execute o programa abaixo.
Clique nos tipos de entrada switch para que
tenha os seguintes valores A1A0=00, então 00,
10, 00, 01, 11, 00. Aperte o botão “Break” e gere
o diagrama de tempo e observe o
comportamento. */

void main()
{
   while (1) { 
      B0 = A0 && A1;
   }
}
