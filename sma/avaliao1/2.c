#include "rims.h"


int t ,x = 0;
void main()
{
   while (1) { 
       if (A0 == 1) //verifica se houve um pedido
            B0 =0;
      
           if (A1==1){//verifica se tem bebida no Barril.
                x++; // contador de dose         
               B0 = A0 && A1;// Sinal que indica que um pedido está sendo atendido
               for(t=0; t<5 ; t++)// temporizador para caracterizar o atendimento 
               
               x;
               
           }else{
           B1 = !A1&&A0;// Sinal que diz ao gerente que é hora   de    comprar mais bebida
           
           }
               
       
       
       
   }
}
